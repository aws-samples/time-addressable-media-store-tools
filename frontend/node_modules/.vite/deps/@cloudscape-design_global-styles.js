//#region node_modules/@cloudscape-design/global-styles/index.js
var _a;
var Mode;
(function(Mode) {
	Mode["Light"] = "light";
	Mode["Dark"] = "dark";
})(Mode || (Mode = {}));
var Density;
(function(Density) {
	Density["Comfortable"] = "comfortable";
	Density["Compact"] = "compact";
})(Density || (Density = {}));
var Theme;
(function(Theme) {
	Theme["Default"] = "default";
	Theme["VisualRefresh"] = "visual-refresh";
	Theme["OneTheme"] = "one-theme";
})(Theme || (Theme = {}));
var themeClassNames = (_a = {}, _a[Theme.Default] = "", _a[Theme.VisualRefresh] = "awsui-visual-refresh", _a[Theme.OneTheme] = "awsui-one-theme", _a);
function hasValue(allValues, mode) {
	return Object.keys(allValues).some(function(key) {
		return allValues[key] === mode;
	});
}
function toggleClass(target, className, enabled) {
	if (enabled) target.classList.add(className);
	else target.classList.remove(className);
}
function applyMode(mode, target) {
	if (target === void 0) target = document.body;
	if (mode && !hasValue(Mode, mode)) {
		console.warn("Mode \"".concat(mode, "\" is not supported"));
		return;
	}
	toggleClass(target, "awsui-dark-mode", mode === Mode.Dark);
}
function applyDensity(density, target) {
	if (target === void 0) target = document.body;
	if (density && !hasValue(Density, density)) {
		console.warn("Density \"".concat(density, "\" is not supported"));
		return;
	}
	toggleClass(target, "awsui-compact-mode", density === Density.Compact);
}
function applyTheme(theme, target) {
	if (target === void 0) target = document.body;
	if (theme && !hasValue(Theme, theme)) {
		console.warn("Theme \"".concat(theme, "\" is not supported"));
		return;
	}
	for (var _i = 0, _a = Object.keys(themeClassNames); _i < _a.length; _i++) {
		var themeValue = _a[_i];
		var className = themeClassNames[themeValue];
		if (className) toggleClass(target, className, theme === themeValue);
	}
}
function disableMotion(disabled, target) {
	if (target === void 0) target = document.body;
	toggleClass(target, "awsui-motion-disabled", disabled);
}
//#endregion
export { Density, Mode, Theme, applyDensity, applyMode, applyTheme, disableMotion };
